// Contagem de Dígitos:
// Escreva um programa que conte quantos dígitos há em um número inteiro fornecido pelo usuário.

const n = 100424543

function contaDigito(n) {
    return n.toString().length
}

console.log(contaDigito(n))

